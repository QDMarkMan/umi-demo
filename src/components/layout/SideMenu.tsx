import React from 'react';

export default (): React.ReactElement => {
  const style = {
    width: '100px',
  };
  return (
    <div className="App-side-menu" style={style}>
      menu
    </div>
  );
};
